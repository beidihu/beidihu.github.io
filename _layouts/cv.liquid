---
layout: default
---
{% unless site.data.resume %}
  <div class="post">
    <article>
      <embed
        type="application/pdf"
        src="{{ page.cv_pdf | prepend: 'assets/pdf/' | relative_url}}"
        width="100%"
        height="100%"
      >
    </article>
  </div>
{% endunless %}
